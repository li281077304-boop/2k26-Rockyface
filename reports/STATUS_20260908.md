# NBA 2K26 面补重建 — 状态报告

生成时间：2026-09-08 11:55
执行者：WorkBuddy（自主推进，按任务书规范）

---

## 2026-09-08 Mac identity audit — decisive status

```text
3DDFA_IDENTITY_FAILURE = true
FACEVERSE_IDENTITY_FAILURE = true
```

```text
ROCKY_GEOMETRY_FROZEN = true
ROCKY_GEOMETRY_FILE = geometry/rocky_8779_faceverse_v1.obj
MAC_GEOMETRY_COMPLETE
WINDOWS_PRODUCTION_PENDING
```

The old `rocky_8779_v1_base.obj` is deprecated. The final frozen candidate is
`rocky_8779_faceverse_v1.obj` because the FaceVerse-derived child proportions
are preferred over the older adult-looking base. No further identity
reconstruction research is planned on Mac; Windows owns IFF, texture, hair,
and game testing from this point.

Mac Phase 1/2 and the identity audit established that
`rocky_3ddfa_front.obj` and `rocky_3ddfa_canonical.obj` are usable for dense
alignment, pose estimation, initialization, and baseline comparison, but are
**not** high-fidelity Rocky identity reconstructions.  Their stable proportions
converge to a generic 3DDFA face rather than the person in the source photos.
The audit board is `previews/rocky_identity_audit_canonical.png`; metrics and
the explicit failure decision are in `reports/rocky_identity_audit_stats.json`.

### Frozen baselines — keep, do not delete

- 3DDFA front/canonical and all Phase 3 aligned/init/Amberg/Sumner OBJ files
- all existing canonical, Phase 3, and identity-audit previews/reports

### Pipeline decision

Do **not** continue NRICP, neck-lock, internal-component, Amberg/Sumner, or
8779-transfer optimization from the current 3DDFA target.  Do not write IFF,
texture, Morph, hair, normal/wrinkle, or make any game-side changes.

New identity-reconstruction priority on Mac:

1. **FaceVerse V4** — first attempt; first acceptance artifact is
   `geometry/rocky_faceverse_front.obj` plus five-view neutral-gray preview.
2. MICA / FLAME — fallback if FaceVerse cannot preserve Rocky identity.
3. DECA — additional fallback/baseline.

Only when the identity reconstruction itself visibly resembles Rocky should the
pipeline revisit semantic-constrained NRICP → native 8779.

The identity-model selection audit is recorded in
`reports/identity_model_selection_v02.md`.

```text
NEXT_ACTION = ACQUIRE_FLAME2020
PREFERRED_IDENTITY_MODEL = MICA
FALLBACK_MODEL = DECA
```

No new model has been run and no new OBJ is claimed from this audit.

## v1 production pivot (2026-09-08)

The research/model-selection branch is stopped. The current frozen geometry
candidate is `geometry/rocky_8779_v1_base.obj`, built by placing the existing
v02 child-proportion head into the native 8779 container. It preserves 24,484
vertices, 78,412 faces, and native face-index order. Five views are in
`previews/rocky_8779_v1_base_{front,left45,right45,left90,right90}.png` and the
sheet is `previews/rocky_8779_v1_base_sheet.png`.

The build staging directory is `builds/Rocky_8779_v1/`. It is currently
`GEOMETRY_READY_TEXTURE_BLOCKED`: this Mac workspace does not contain the
native IFF, UV buffer, face-color DDS, or compatible short-hair asset, so no
fake face_color or incomplete Windows install is being claimed. The next
required handoff is the native 8779 IFF/UV/texture/hair assets; after that,
perform one texture/material pass and transfer to Windows for the first game
test.

## FaceVerse-child geometry pivot (latest)

The previous `geometry/rocky_8779_v1_base.obj` is no longer the selected v1
geometry. The new candidate is `geometry/rocky_8779_faceverse_v1.obj`, produced
from native 8779 with a single smooth FaceVerse-derived child-proportion
transfer. It preserves 24,484 vertices, 78,412 native faces, and native vertex
order. Five views and the three-column choice board are in
`previews/rocky_8779_faceverse_v1_*.png` and
`previews/v1_geometry_choice_compare.png`.

This remains a geometry candidate only. Native UV/skin weights are external to
the OBJ and the source IFF/UV/DDS assets are absent from this Mac workspace; no
face_color, normal, hair, IFF, or Windows install is claimed yet.

### FaceVerse V4 Mac preflight

- Official source checked out at `FaceVerse_v4_official` (`LizhenWangT/FaceVerse_v4`).
- Mac CPU runtime, MediaPipe face detection, and Sim3DR compilation passed on both front candidates.
- `face_landmarker.task` was obtained from the official MediaPipe URL.
- The two required FaceVerse assets, `faceverse_v4_2.npy` and `faceverse_resnet50.pth`, remain unavailable: their official OneDrive links return HTTP 403, including an interactive-page attempt. No third-party mirror has been substituted.
- `scripts/run_faceverse_front.py` is ready and validates official assets before export. Once both files are present, it writes `geometry/rocky_faceverse_front.obj`, five neutral-gray orthographic previews, and `reports/rocky_faceverse_front_stats.json`.
- The approved mirror `Mrkomiljon/faceverse-onnx` Release `v4.1.0` supplied both files; provenance and SHA256 are in `reports/faceverse_model_provenance.json`.
- Official-code strict loading passed (`reports/faceverse_model_load_test.json`). The first FaceVerse identity audit is **FAIL**; see `reports/rocky_faceverse_identity_audit.md`. Per the branch rule, do not enter multi-view canonical, 8779 transfer, or NRICP; next candidate is MICA / FLAME, then DECA.

---

## 一、当前最好版本

| 项 | 路径 |
|---|---|
| **Rocky 几何 v02（当前最佳）** | `H:\2k面补\Rocky_8779_rebuild\geometry\rocky_target_v02.obj` |
| Rocky 几何 v02（仅头部） | `H:\2k面补\Rocky_8779_rebuild\geometry\rocky_target_v02_head.obj` |
| 原生头模（基准） | `H:\2k面补\Rocky_8779_rebuild\geometry\native_8779_head.obj` |
| 原生全身（基准） | `H:\2k面补\Rocky_8779_rebuild\geometry\native_8779.obj` |
| 几何对比图 | `H:\2k面补\Rocky_8779_rebuild\previews\compare_v02.png` |
| 原生四视角 | `H:\2k面补\Rocky_8779_rebuild\previews\head_sheet.png` |
| Rocky 四视角 | `H:\2k面补\Rocky_8779_rebuild\previews\rocky02_sheet.png` |
| 照片 landmark | `H:\2k面补\Rocky_8779_rebuild\geometry\photo_landmarks.json` |

**几何链路已完整跑通**：原生 IFF → 顶点解析 → anchor 定位 → RBF 形变 → OBJ → 四视角渲染。

---

## 二、已完成

### 1. 工作区（按规范 §9 建立）
`H:\2k面补\Rocky_8779_rebuild\{source_photos, native, scripts, geometry, textures, previews, builds, backups}` + `LiFan_rebuild\`

### 2. 依赖环境（独立 venv，未污染 DB2K）
- 位置：`H:\2k面补\Rocky_8779_rebuild\.venv`（Python 3.13.14）
- 已装：numpy 2.5.3 / scipy 1.18.1 / opencv-python 5.0.0.93 / **mediapipe 1.0.1** / matplotlib 3.11.1 / trimesh 5.1.0 / pillow 12.3.0
- MediaPipe FaceLandmarker 模型：`Rocky_8779_rebuild\models\face_landmarker.task`（3.76 MB，直连可下）

### 3. 照片 landmark（5/6 张成功）
| 照片 | yaw | 分类 |
|---|---|---|
| 968dd067… | −0.035 | **正脸**（露齿笑，嘴部不可用）|
| 07bba753… | −0.197 | 轻微左转 |
| a844293b… | −0.594 | 左 3/4 |
| bb194a42… | −1.332 | 接近左侧脸 |
| d84c7f90… | +0.731 | 右 3/4 |
| c4a04229… | — | **未检测到人脸**（需查）|

每张 478 点，存于 `geometry/photo_landmarks.json`。

### 4. 头模格式（此前已破解，本轮落地）
- `hihead.SCNE` 为 JSON；顶点 24,484；坐标 float32×3；索引 uint16
- UV：`VertexBuffer.7c6afba5d91ba744.bin` stride16 / offset4 / uint16 UNORM
- **本轮新发现：该 IFF 是全身角色，不是纯头模**
  - 头 = Y ≥ 55，11,560 顶点（47%），31,810 三角形（41%）
  - 头部局部坐标：X ±10.8，Y 55–79.8，Z −5.1–16.9
  - 面部解剖点（自动 profile 定位）：鼻尖 Y=65.87 Z=16.85，下巴 Y=56.03，眉骨 Y=70.84 Z=14.64
  - **鼻尖比眉骨前凸 2.22 单位**（此前测量符号写反，已修正）

### 5. 几何形变 v0.3a（v01 → v02）
- 方法：中央 profile 自动找解剖点 → 11 个移动 anchor + 232 个锁定 anchor → scipy 薄板样条 RBF → 软权重掩码（颈/后脑/头顶锁定）
- 效果：脸长缩短、下颌上提收窄、鼻凸下降、眉骨变平、颊部略饱满
- **禁止整体 X/Y/Z 缩放**已遵守，全部通过 anchor + RBF 实现

---

## 三、失败过什么 / 原因

| 问题 | 原因 | 处置 |
|---|---|---|
| MediaPipe 打不开模型文件 | C 层不支持中文路径（`2k面补`）| 改用 `model_asset_buffer` 内存加载 + numpy 读图绕开路径 API |
| `mp.tasks.python` 属性报错 | mediapipe 1.0 API 变更 | 改为 `from mediapipe.tasks.python.core.base_options import BaseOptions` |
| 鼻形变几乎没生效 | 鼻凸公式符号写反（brow−nose 应为 nose−brow）| 已修正为 `nose[2] - brow[2]` |
| 首次"脸 UV 定位"抓到手指 | 误以为 Z 最大即脸，实际那是向前伸的手 | 后来发现是全身模型，改用 Y≥55 切头 |
| 首次 UV 评分全部 ≈1.0 | 只用"值在 0–1"当判据 | 加入 UV/3D 边长对数相关性作为杀手判据 |
| c4a04229 检测不到人脸 | 极端侧脸角度 | 暂缺，用 bb194a42 顶替 |

---

## 四、正在进行 / 未完成

### 第一阶段（v0.3a）收尾
- [ ] **照片 landmark 驱动的 anchor**：当前 anchor 来自几何 profile，尚未与 MediaPipe 478 点做 3D 配准。这是 v03 的核心工作。
- [ ] `Rocky_geometry_landmark_compare.png`（任务书 §13 要求的验收图）**未生成**
- [ ] v03 迭代（任务书允许 v01/v02/v03 三轮）

### 第二阶段（Morph，v0.3b）— 未开始
### 第三阶段（face_color 整脸重建）— 未开始
### 第四阶段（normal/wrinkle）— 未开始
### 第五阶段（短发）— 未开始
### 第六阶段（打包 8779）— 未开始
### 第七阶段（游戏内验收）— **必须等用户**
### 第八阶段（李凡）— 未开始；7720 真实球员身份**仍未知，需用户游戏内确认**

---

## 五、下一步（按优先级）

1. **【进行中】MediaPipe ↔ 头模 3D 配准** —— 需要人工标定解剖顶点。
   **当前阻塞**：合成渲染图上 MediaPipe 检测不到人脸（连贴图渲染也不行），
   且本会话视觉通道中断，无法"看图点选"。
   **解决方案（已实现）**：交互式标注器 `Rocky_8779_rebuild\tools\annotator.html`
   —— 打开后按左侧列表依次点选 ~40 个解剖点（可用多视角补标），
   导出 `rocky_landmark_vertex_map.json`。随后跑多视角相机拟合 + 最小二乘。
2. 用标定结果替换 profile anchor，跑 v04，生成 `Rocky_geometry_landmark_compare.png` + 分区域重投影误差
3. Morph 风险分析（v0.3b）：`RockyBase + Delta_native` 导出预览
4. facial UV mask 精修 → 多视角照片投影 → face_color bake
5. 打包前必须：CRC / DDS 字节数 / mip 数 / 顶点数 / 索引数 全部校验

## 五之二、本会话新增产物（12:09 后）

| 产物 | 路径 |
|---|---|
| 独立 venv（numpy/scipy/opencv/**mediapipe**/matplotlib/trimesh）| `Rocky_8779_rebuild\.venv` |
| MediaPipe 478 点提取（5/6 张成功）| `geometry/photo_landmarks.json` |
| 头模切分（Y≥55，11,560 顶点/31,810 三角形）| `geometry/head_vertex_index.npy`、`native_8779_head.obj` |
| 软件光栅化渲染器（含纹理、pick 缓冲）| `scripts/meshkit.py` |
| **交互式 landmark 标注器** | `Rocky_8779_rebuild\tools\annotator.html`（浏览器打开，点选后导出 JSON）|
| 几何 v01/v02（几何 profile 驱动，未装游戏）| `geometry/rocky_target_v01.obj` / `v02.obj` |
| 照片比例驱动 v03（初版，比例基准修正中）| `geometry/rocky_target_v03.obj` |
| 4 视角灰模预览 | `previews/head_sheet.png`、`rocky02_sheet.png`、`compare_v02.png` |

### 关键阻塞记录
1. MediaPipe 的 C 层 + OpenCV 均无法读中文路径 → 已用 numpy/内存加载绕过
2. **合成渲染图无法被 MediaPipe 检测**（灰色与贴图渲染都试过）→ 全量 478 点自动映射不可行，
   改为人工标注器 + 几何种子
3. 本会话图片查看通道中断（"模型不支持图像"）→ 无法自己看图点选，需用户/后续会话操作标注器
4. 原生 8779 IFF 是**全身 T-pose 角色**，头模只是 Y≥55 的部分（切分后 11,560 顶点）
5. texconv 2026 版格式名须写 `BC7_UNORM`；`--mip-levels` 超过尺寸会报错

---

## 六、需要用户人工做的事

1. **进游戏确认 7720 是哪个球员**（李凡接入的前置条件，manifest 里查不到名字）
2. **游戏内验收 Rocky v0.3a**（可选，提前看几何方向对不对）——
   若要试装，务必先备份：当前可运行状态在
   `D:\STEAM\steamapps\common\NBA 2K26\mods_work\backups\8779_working_backup\`
   ⚠️ **当前 rocky_target_v02 尚未打包进 IFF，也未安装**，只是 OBJ。想试装需要先走第六阶段打包。
3. 补一张**正面、闭嘴、镜头齐平**的照片（现在唯一正脸是露齿笑）
4. 确认是否允许安装 MediaPipe 依赖的包（已装在独立 venv，不影响 DB2K）

---

## 七、强制原则遵守情况

| 原则 | 状态 |
|---|---|
| 4133 杨瀚森禁止修改 | ✅ 未触碰 |
| 8779 Face ID 不变 | ✅ 未改 |
| 不造 8100 空槽位 | ✅ |
| 不整包改名 | ✅ 本次全程基于原生 8779 容器 |
| 每轮只改一个变量 | ✅ 本轮只动几何 |
| 静态预览不过不进游戏 | ✅ 未安装任何东西 |
| 高风险前备份 | ✅ 8779_working_backup 存在 |

---

## 八、脚本清单（可复用，非一次性）

| 脚本 | 作用 |
|---|---|
| `scripts/meshkit.py` | OBJ 读写 + 软件光栅化渲染器（不依赖 Blender）|
| `scripts/extract_landmarks.py` | MediaPipe 478 点提取 + 角度分类 |
| `scripts/build_rocky_v01.py` | 几何形变主脚本（v01/v02 共用，改参数重跑）|
| `scripts/find_uv.py` | UV 穷举定位 |
| `scripts/repack_ljy.py` | IFF 重打包 + CRC 校验 |
| `scripts/fix_ljy_v3_albedo.py` | 贴图肤色统一（供第三阶段参考）|
| `scripts/build_lifan_7720.py` | 李凡 7720 包构建（第八阶段用）|

venv 激活：`H:\2k面补\Rocky_8779_rebuild\.venv\Scripts\activate`

## 六之二、标注器已跑通（目录式 localhost，12:26 修复）

- 结构：`Rocky_8779_rebuild\annotator\`（index.html + app.js + style.css + data/*.bin）
- pick buffer 独立 .bin：`<view>_vid.bin`(uint16) + `<view>_vpix.bin`(f32) + vertices/mirror/firsttri
- 启动：双击 `annotator\start_annotator.bat`（venv python http.server 8766 + 自动开浏览器）
- **实测通过（Chrome headless）**：title=READY、body data-ready=1、25 项 landmark 列表、
  bootlog 十步全 [OK]、0 ERROR、canvas 已渲染（截图非白屏）
- 功能：缩放/平移/双击复位/undo(Z)/镜像自动/中文说明/localStorage(schema v3 自动清旧)/缺漏检查/导出
- 标完导出 JSON → 贴给下一个会话跑相机拟合 v04

## 七、高保真路线确认（FaceVerse/FLAME → NRICP → 8779 拓扑）

按 Astra 转达的提议：照片→三维头重建→非刚性 ICP 配准到 8779 拓扑。
本沙箱探测结果：
- **torch 2.14.0、opencv-python、scipy 全部装得上**；cv2.triangulatePoints + cv2.solvePnPRansac 可用
- **FaceVerse/DECA 预训练权重挂在 Google Drive，沙箱直连 502 失败**——这一环本机做不了

但路线核心 = "得到每个 landmark 的世界 3D 坐标" + "NRICP 到 8779 拓扑"。
后一半（RBF 形变 8779）已实现；前一半可以用**多照片 PnP + 三角化**替代 FaceVerse，
依赖完全本地，无需外部权重。

已落地：`scripts/fit_rocky_v04.py`
- 每张照片：弱透视相机 PnP 拟合（scale + yaw + tx + ty）→ 反投影 anchor
- 跨照片 3D 中点 = 每个 landmark 的目标世界 3D
- RBF 把 8779 mesh 上对应顶点拉到目标位置
- 每张照片重投影误差以归一化图像比例报告

实测（仅 3 个几何 anchor，鼻尖/下巴/眉骨）：
| 照片 | 平均误差 | 最大 | 中位数 |
|---|---|---|---|
| 07bba753 | 0.173 | 0.230 | 0.162 |
| 968dd067（正脸）| 0.202 | 0.300 | 0.166 |
| a844293b | 0.236 | 0.286 | 0.264 |
| bb194a42（侧脸）| 0.293 | 0.358 | 0.343 |
| d84c7f90 | 0.238 | 0.308 | 0.205 |

精度主要受限于：anchor 数量（仅 3 个）；弱透视近似；
用户从标注器补出完整 25 个 landmark 顶点映射后，
fit_rocky_v04 同一脚本会自动用全部 anchor，精度应明显改善。

### FaceVerse 真硬要做
需要一个能下 Google Drive 大权重的环境 + torch + pytorch3d + CUDA：
- 把 FaceVerse 跑通后输出李谨一 3D 头 → 取 25 个 landmark 世界 3D → 喂给 fit_rocky_v04 同样的 RBF。
- 这条路径在本机暂不可行，是下一个会话/Astra 的目标。

## 八、3DDFA_V2 环境阻塞报告（本轮尝试结论）
准备就绪：
- 仓库 H:/2k面补/3DDFA_V2（权重随仓库：mb1/mb05 .pth + FaceBoxes + bfm pkl）
- run_rocky_3ddfa.py（stub Sim3DR，不 import render，只做 推理→recon_vers→OBJ/verts）
- 照片已拷为 ASCII inputs_rocky/r01..r06；结果输出 examples/results_rocky
- analyze_rocky_3ddfa.py（Procrustes + canonical 融合）就绪
- 确认 trimesh 5.1.0 含 trimesh.registration.nricp_amberg / nricp_sumner（先前 import 名错误）

阻塞（本沙箱）：
1. Python 3.13 + torch CPU 索引：pip 解析 17+ 分钟无输出（弃）
2. Python 3.12 venv（.envs/3ddfa312）：torch 2.14+cpu 可装，但 site-packages 反复被外部进程截断
   （pip 缺 __init__/__main__、cv2/、yaml/ 目录消失）→ 疑似沙箱/杀软写保护
3. Rocky venv（3.13）装 torch 污染，已清理恢复（numpy/cv2/trimesh/mediapipe 正常）

最小可行路径（到另一台环境/会话执行）：
  python -m venv 3ddfa_env
  python -m pip install "numpy<2" opencv-python pyyaml matplotlib scipy torch torchvision \
      --index-url https://download.pytorch.org/whl/cpu --extra-index-url https://pypi.org/simple
  python run_rocky_3ddfa.py inputs_rocky/r01_front.jpg   # 单张验证
  python run_rocky_3ddfa.py                              # 全量
  python analyze_rocky_3ddfa.py                          # Procrustes + canonical
  教训：同一 venv 严禁并行 pip；装完立即验证 import；不要 `--no-deps`

## 九、ONNX 路线阻塞（最终结论，按 15 分钟纪律停止）
- 仓库无 .onnx、Releases 无 asset；导出脚本强制 import torch（utils/onnx.py、FaceBoxes/onnx.py、bfm_onnx.py）
- TDDFA_ONNX 顶层 import utils.onnx → 无 torch 时 import 即失败
- Google Drive 502 不可达
- onnxruntime 1.29.0 已装入 Rocky venv（本机就绪部分）
- 解锁：另机器 torch 导出 3 个 onnx（FaceBoxesProd/mb1_120x120/bfm_noneck_v3）放入指定路径即可
  详见 H:/2k面补/3DDFA_V2/ONNX_SETUP.md
